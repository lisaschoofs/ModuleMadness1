function randomNumber(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min)) + min;
 }

//when we call this module, use 100 as min and 1000000 as max

module.exports = randomNumber;
