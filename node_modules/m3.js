// The third module should require the other two modules (i.e. Module One and Module Two).
// It should have two function exports:
//
// The first function export should return the result of the first
// module being passed into the second module.
// The second function export should return the text “Account balance: \n”.

var randomNumber = require('m1.js');
var convertNumber = require('m2.js');

var first = randomNumber(100, 1000000);

// return result of first module being passed into the second
function fun (number){
  var finalNumber = convertNumber(first);
  return finalNumber;
}

// function fun (){
//     return "$123,000";
// }


// export should return text "Account balance: \n""
function verbage() {
  return "Account balance: \n";
}


// function
//
// function

module.exports = {
  fun, // return result of first module being passed into the second
  verbage // export should return text "Account balance: \n""
};
